#include <iostream>
using  namespace  std;

int media(n);
    for (int j=0; j<30; j++){
        if (j<15){
            notas[j] = 10;
        }else{
            notas[j] = 10;
        }

        soma = 0;
        for (int i=0; i<30; i++){
            soma += notas[i];
        }
        return media = soma/30;
    }